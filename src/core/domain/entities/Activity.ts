import { BaseEntity } from "@/core/domain/entities";

class Activity extends BaseEntity {
// title, description, Image, DayOfWeek, Date, StartTie, EndTime, placename, location targetaudience, maxvolunteers 
}