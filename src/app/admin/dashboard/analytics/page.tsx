import { AnalyticsPage } from "@/presentation/pages";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "الإحصائيات",
};

export default function Page() {
  return <AnalyticsPage />;
}
