interface FeaturedPostRequest {
  id: string;
  name: string;
  title: string;
}

interface FeaturedPostResponse {
  id: string;
  name: string;
  title: string;
}

export interface ActivityRequest {
  id: string;
  name: string;
  title: string;
}

export interface ActivityResponse {
  id: string;
  name: string;
  title: string;
}


// Request has main 3 things 
// http first line
// header ( cockies, tocken, etc... )
// body ( json, xml , blain text )


// Response has main 3 things 
// http first line
// header ( cockies, tocken, etc... )
// body ( json, xml , blain text )